#!/usr/bin/env node
/**
 * Placeholder for react-mentions postinstall patches (e.g. compatibility fixes under `node_modules`).
 * Keeps `yarn install` succeeding when no patch is required.
 */
process.exit(0);
